@using Stolarus2.Data.Properties
@using Stolarus2.Data.Models

@{

    var values = Enum.GetValues(typeof(ArticleCategory)).Cast<object>()
                     .Select(v => new SelectListItem
                     {
                         Selected = v.Equals(Model),
                         Text = v.ToString(),
                         Value = v.ToString()
                     });
}

@Html.DropDownList("", values, string.Format("< {0} >", Resources.SelectArticleCategory), new { @class = "form-control" })